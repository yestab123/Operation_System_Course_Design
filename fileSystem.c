bool creat_file(file_name, file_type){
	
	//只读文件则建立失败
	if(file_type == "read_only") {
		return false;
	}

	//父目录不存在则建立失败
	if() {
		return false;
	} else {
	
		//存在重名文件则建立失败
		if() {
			return false;
		} else {

			//建立文件目录，并分配磁盘块


			//填写目录和已打开文件表
		}
	}

}